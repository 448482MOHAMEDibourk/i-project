name: ai-school CI

on:
  push:
    paths:
      - "new-project/ai-school/**"
  pull_request:
    paths:
      - "new-project/ai-school/**"

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r new-project/ai-school/requirements.txt
      - name: Run smoke
        run: |
          python new-project/ai-school/run_ordered_prompt.py
